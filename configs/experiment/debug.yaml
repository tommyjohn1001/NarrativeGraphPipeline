# @package _global_

defaults:
  - override /logger: null

task: debug

seed: 307
batch_size: 5
num_workers: 1

trainer:
  gpus: 1
  min_epochs: 1

datamodule:
  batch_size: ${batch_size}
  num_workers: ${num_workers}
